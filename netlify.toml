[build]
command = "rustup toolchain install nightly-2026-05-01 --component rust-src --component rustc-dev --component llvm-tools-preview --component miri && cargo install mdbook --version 0.5.2 --locked && cargo install mdbook-katex --git https://github.com/lzanini/mdbook-katex && cargo install mdbook-aquascope --locked && cargo +nightly-2026-05-01 install aquascope_front --git https://github.com/cognitive-engineering-lab/aquascope && cd sitp && mdbook build"
publish = "sitp/book"
